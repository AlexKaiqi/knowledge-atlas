# Agent 接手与维护指引

本文件面向参与项目开发、内容整理和知识维护的 Agent。用户可以 clone 后用自己的 Agent 工作；本项目提供知识、探索资产和薄接入约定，不自研全套 Agent。

## 用户意图与产品方向

先分析用户根本目标，不局限于字面理解；不要默认方案、术语和前提正确。合理就推进，不合理应指出并给出更适合的路径。避免无用解释；用户纠正时先重构目标和任务边界。

产品目标以 [PRODUCT.md](PRODUCT.md) 为准：问题引导、逐步深入的科普、自由探讨与实践、长期 wiki 共建。目标体验是用户直接表达意图，人与 Agent 围绕持续探索共同工作，按需创建解释、工具与实验成果。手机与语音应容易使用。现有工程案例不能限制知识领域。

模型、登录、会话、规划、工具循环与调度由使用者选择的已有 Agent 负责。接入优先通过 `.agents/skills/`、配置和说明，不另写 Agent Provider、模型代理、云调度平台或全套语音服务。此前要求“先接真实 Agent 闭环”的路线已撤回，不作为待完成工程。

稳定协议约束身份、权限、版本和溯源；正文格式保持自由。不要给自由知识新增统一层数、题型或字段数量门槛，也不要把学习方法变成强制关卡。

## 接手时先读什么

1. [task.md](task.md)：当前事实、优先项、依赖、验收和验证记录。不要仅从 README 的愿景判断已有能力。
2. [PRODUCT.md](PRODUCT.md)：理解本次修改应服务的体验与目标。
3. [ARCHITECTURE.md](ARCHITECTURE.md)：确认相关模块、数据归属和实际运行边界。
4. 按任务阅读 [探索服务设计](docs/EXPLORATION_SERVICE.md) 的对应部分；涉及知识、导学、实验或 AI 引导时，再读 [知识审阅规范](docs/KNOWLEDGE_REVIEW.md) 和 [方法调研](docs/LEARNING_METHODS.md)。

使用自己的 Agent 见 [接入说明](docs/AGENT_USAGE.md)；维护实验、环境或数据时读 [探索资产维护](docs/EXPLORATION_ASSETS.md)。

先检查 `git status` 与相关代码；保留已有修改，不覆盖其他工作。遇到更具体的目录维护说明时一并阅读。详细设计留在对应文档，路线与状态集中维护于 `task.md`。

## 当前能力必须如实理解

当前已实现工作区、持久化、共同讨论、成果版本、知识修订、内置模型与本地 Docker 执行。仓库提供共享 Skill、知识/探索目录和可复用环境入口。Codex CLI 两次真实会话、Docker 与网页成果往返已验收，范围见 [验收记录](docs/ACCEPTANCE.md)；Pi/OpenCode 尚未实测。**本地网页已通过现有 Codex CLI 直接回答；托管服务未接通。** 可用时新问题原子保存并创建真实 Codex 任务；未可用时明确显示原因。`waiting_provider` 兼容状态表示旧有或显式保存的待办，不是正在推理。

本地薄桥接使用 `server/codex-{cli,app-server,jobs,progress,runner}.mjs`，复用原生 app-server 和现有任务状态，不实现模型或工具循环。`browser-workbench.mjs` 接入现成 AIO Sandbox，`browser-proxy.mjs` 保护 browser-ui 的 CDP 连接。只发送当前探索有限上下文与 Skill 指导；关闭宿主 Shell、Skills、插件、桌面工具和宿主 MCP，仅开放显式授权的本段探索远端工具。本机只读与远端工作目录可写须明确区分，不能关闭模型所需的原生 code_mode_host。不得把用户数据库、研究正文、其他探索、私人偏好或桌面工具管道交给子进程。不要把本机 Codex 登录直接暴露为公网服务。版本、资产和真实验收边界见 [浏览器接入](docs/BROWSER_WORKSPACE.md)。

同一数据库只允许一个本地 serve 进程。取消时除了原生 turn/interrupt 和条件写回，还要停止对应环境中的后台进程；清理不得停止较新租约的工作。浏览器组件卸载须先断开 CDP transport，不能直接调用会关闭远端标签的上游 destroy；重连与切探索必须检验页面内存状态保留。测试使用独立数据库和容器命名空间。

不能把执行器当成 Agent，也不能把 Skill 存在当成真实接入测试通过。`RUNTIME.md` 仅为既有教学实验契约；`AGENT_INTEGRATION.md` 指向现行薄接入，旧服务端 Provider 设计已撤回。

响应式和浏览器语音代码已实现，但真机体验尚待验收。平台网关账号适配、本地 Docker、访问触发的任务恢复，不等于已验证跨设备身份、生产云沙箱或持续云端调度。后续接入后同步更新这里与 `task.md`，不要保留过时状态。

## 改动应落在哪里

| 任务 | 主要维护位置 |
| --- | --- |
| 自己的 Agent 与项目接入 | `.agents/skills/knowledge-atlas/SKILL.md`、`docs/AGENT_USAGE.md`；共享一份规则入口 |
| Git 自有知识与探索 | `knowledge/`、`explorations/`；正文自由，保留来源和稳定引用 |
| 网页环境共建 | `server/environments.mjs`、`site/assets/workspace-environments.js`、`environment-package.js`；创建/派生/加入/修订/分享与冻结快照 |
| 可复用环境与数据恢复 | `environments/`、`docs/EXPLORATION_ASSETS.md`；复用原生工具 |
| 网页直接回复与 CLI 边界 | `server/codex-cli.mjs`、`server/codex-jobs.mjs`、`server/codex-runner.mjs`；对话指导在 Skill 的 `references/conversation.md` |
| 场景、导航与知识阅读 | `docs/PRODUCT_SCENARIOS.md`、`site/assets/workspace-navigation.js`、`workspace-scenes.js`、`public-knowledge.js`；保持同一工作区壳 |
| 工作区与手机交互 | `site/pages/workspace.mjs`、`site/assets/workspace.js`、`site/assets/workspace.css` |
| 请求、权限与服务数据 | `server/api.mjs`、`server/workspace.mjs` |
| 任务状态、结果提交与恢复 | `server/jobs.mjs` |
| 本地隔离执行 | `server/docker-runner.mjs` |
| 本地服务与托管适配 | `scripts/serve.mjs`、`server/local-db.mjs`、`server/worker.mjs` |
| 数据结构与迁移 | `db/schema.ts`、`drizzle/`；生成迁移用 `npm run db:generate` |
| 方法及调研来源 | `content/learning/methods.json`，同步对应文档与展示 |
| 既有种子知识与导学 | `content/knowledge/`、`content/learning/`、`content/cases/` |

侧栏不要同时放「我的探索」和同义的新建入口；默认空首页统一到我的探索，对话内新问题及 Cmd/Ctrl+K 只回到此输入框，保留草稿并关闭手机导航。

「我的探索」必须可以直接提问并用 Enter 启动新探索，历史搜索不能占据主输入位置；复用同一文字/语音控制器，首页新问题与原讨论草稿分开保存。

切换目录与切换探索是独立动作。新入口要更新主内容与 URL，保留草稿及返回位置；异步完成不能覆盖已切换的场景或关闭新开的表单。知识引用要可见、可移除，发送前核对服务知识的当前分享范围。实践明确目标范围，分步失败重试复用已完成步骤。不要恢复相互割裂的百科和探索主界面。

`dist/` 是生成产物，不直接修改。方法内容统一维护于 `content/learning/methods.json`，新的方法须区分研究效果证据、课程/产品设计借鉴及本项目推论。

## 数据与知识维护

Git 自有知识、探索与环境定义直接在仓库维护，不要求启动服务。服务中的探索、正文修订、成果与学习记录以 SQLite/D1 为权威，通过服务接口和迁移维护。服务导出是工作副本，改文件不回写数据库；整理为 Git 自有派生条目时记录源 ID、版本及分享范围。没有自动双向同步，不让两处同时覆盖同一权威正文。现有 JSON 是种子适配层，改 JSON 不会修改服务正文。新目录尚未自动进入网页索引。

新增或实质修改知识时，判断它在声明范围内是否能解释问题、给出依据、说明条件与未知，并提供继续检验的方向。这是内容责任，不要求固定章节。对相关学习方法说明适用性、检查依据及“满足 / 需改进 / 不适用”的理由，记录实际审阅者为 Agent 或人及尚待完成的工作。

保留来源、原作者、异议与修改理由；知识关系记录方向、理由和对应版本，相似关系不能当成证据支持。缺少来源或尚未完成的想法留在明确标注的草稿/讨论中，不伪称已验证。结构检查、编辑自查、独立内容审阅和真人试学分别记录，不能互相冒充。`maintained` 表示纳入维护，不代表主张已被证实或 Agent 已在自动巡检。

接续材料只包含明确选定的成果版本及其自身元数据，不能按成果 ID 扩大携带历史任务输入。导入表单绑定打开时的探索，切换后不得把旧表单保存到新目标。

公共讨论、修订提案、成果和私人学习记录分开处理。只按已有权限和明确选择的范围分享；不把私人会话、历史草稿或学习画像自动带入公共知识。身份由服务端确定，昵称或请求字段不能授予角色权限。

分享知识只开放明确共享的修订，不能追溯公开此前私稿；关系目标转为私人后，读取和导出都要检查当前权限。重构不能破坏这些行为。

通过基础版本和修订流程提交变更，遇到并发修改先比较并合并，不覆盖历史。普通维护在用户授权范围内推进；证据不足的争议保留为待办，不捏造已解决的结论。

## 执行与长期可靠性

实验保存必须保留冻结输入、运行条件、结果和独立历史；再次运行产生新记录。生成面板与主应用隔离，执行环境不直接持有生产数据库、部署凭据或无关用户资料。

环境允许参与者从需求新建或派生，不局限在现成实验。网页环境以独立 SQL 版本资产维护：私人历史不随分享公开，共建先加入再按基础版本修订；带到探索冻结文件。探索分享还检查全部历史私人环境快照的公开许可。导入只预览，不自动执行；返回包先合并再存新版本，完整规则见环境资产文档。

环境定义、镜像、运行实例、持久数据和结果分开维护。复用少量环境，每段探索独立写入区；重要运行记录实际代码/环境版本与平台。容器内的有效改动回写定义，输出和数据保存在容器外。卷不是备份，`docker commit` 不包括挂载卷；有状态环境要记录备份与实际恢复验证。`.local/` 可能有唯一资料，不按缓存清理，也不全局清理未知归属的 Docker 卷。

保持请求幂等、任务租约、取消和权限边界。新增可执行能力必须验证实际运行、取消和恢复；过期执行者不能提交结果，失败须有真实可检查状态。不用浏览器本地状态伪装共同数据库，不把固定提示伪装成在线 AI。

数据库改动使用追加式迁移，不重写已应用迁移。保留现有记录与接口兼容性，迁移需要考虑失败恢复；构建不能清理 `.data/`。凭据和用户数据不进入源码或发布包。构建、测试通过和部署成功分别记录。

## 验证与交接

- 新增或修改知识、导学、实验、AI 引导及应用行为后，运行 `npm run check`；验证应覆盖本次改动涉及的真实行为。
- 修改 Docker 执行时，在有可用环境的情况下运行 `ATLAS_TEST_DOCKER=1 node --test tests/workspace.test.mjs`。环境不可用时明确未验证的部分，不伪报通过。
- 修改可复用环境时验证它自己的实际构建、运行、退出及数据留存/重建；网页 runner 的测试不能替代环境验收。Skill 格式校验与真实 Agent 使用分别记录。
- 数据与权限测试使用独立临时数据库，不向用户正在使用的探索写测试内容。
- 仅改说明文档时核对事实、链接及 `git diff --check` 即可；不把旧测试结果写成此次重新运行的结果。
- 手机、语音、弱网、多人编辑与生产行为须分别记录实际验收；代码存在不等于体验已验证。

完成阶段工作、改变方案或发现阻塞后，同步更新根目录 [task.md](task.md) 的状态、验证依据、限制和下一步。产品定位或能力边界改变时同步 README/PRODUCT；架构决策改变时同步对应设计文档。只有实际完成所声明范围的项目才勾选，交接时明确什么已做、什么未做。

## 研究边界

`research/` 与 `formal/` 独立维护；不自动公开研究正文，不把未验证研究写成科普定论，不撤销用户已有改动。涉及这些目录时先阅读其自身说明，不因维护学习平台而批量整理或迁移研究文件。
