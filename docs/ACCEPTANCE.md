# Codex CLI 与产品验收

2026-09-08，维护 Agent 实际执行。**后续用户实测指出：下述文件交接并不等于网页直接问答。直接问答已另行补齐，记录见本文件末尾「网页直接提问与输入模式」。** 原交接验收结论：**自己的 Codex CLI + 仓库 Skill + Docker + 网页成果往返已走通**。这是一个明确问题的功能验收，不代表教学效果、所有 Agent 或生产环境已验收。

## 实际路径

1. 将当前项目的相关文件制作成本地 Git 快照，再全新 clone；排除研究正文、用户数据库、凭据、部署配置与其他私人文件。快照为 `efd76ba2ff6f43ac533c09450ba3545962acd67e`，不是远程仓库已发布版本。
2. 用已登录的 `codex-cli 0.153.4` 实际调用模型。第一会话显式加载 `$knowledge-atlas`，阅读项目规范、已有并行模型和环境，探索“80% 工作可并行，增加到 2、4、8、16 人会怎样”。它记录了 Docker socket 权限拒绝，实际完成宿主 Python 计算，没有把失败写成容器成功。
3. 在独立测试数据库的网页中提出同一问题，运行内置模型，选择当前成果并复制接续材料。材料写入 clone 后，第二个全新 Codex 会话从文件接续，未使用第一会话隐藏聊天历史。
4. 第二会话按显式允许的本机 Docker socket 权限执行 80% 与 50% 两组条件；沿用工作区文件限制，没有用全盘无沙箱模式。实际镜像为 `sha256:7e6554dfac69cf3f73515099986fd2e036d13a079f8bb0188ef26bb120e24147`，linux/arm64、Python 3.13.15。两次计算及各四项检查均退出 0，产生独立冻结输入、代码、命令与输出。
5. 通过真实浏览器文件选择器，把 Codex 生成的 `report.md` 和 `comparison.html` 分别读入表单，检查后保存。HTML 在 `sandbox="allow-scripts"` 的成果 iframe 中运行，实际切换 50% 后显示 `1.882353` 倍、`53.125` 分钟；固定 Docker 表格保持原数据。
6. 在网页修订交互成果，确认 v2 与原 v1 同时存在；刷新后成果和输入草稿仍能恢复。

本机 PATH 中原 Codex 启动器缺少 vendor 可执行文件；实际使用桌面应用自带的 CLI。未修改全局安装、模型账号或用户配置。Docker 权限按本次命令提供；它允许访问 Docker daemon，不等于 daemon 内部的容器级授权。具体使用入口见 [AGENT_USAGE.md](AGENT_USAGE.md)。

## 验证矩阵

| 范围 | 本次实际结果 |
| --- | --- |
| Skill、资料发现、两次真实模型会话 | 完成；留下代码、报告、知识 v1/v2、方法自查与接续说明 |
| 冻结运行与接续 | 完成；独立 Agent 核对 13 个旧运行文件摘要未变，两个新运行的代码/输入/输出摘要匹配，八组精确分数正确 |
| 网页 → Agent | 选择版本、预览与剪贴板复制完成；默认不带整段讨论或私人偏好 |
| Agent → 网页 | 实际文件选择、Markdown 表格显示、自包含 HTML 交互、版本保存与历史完成 |
| 桌面与手机尺寸 | 桌面浏览器和 390×844、320×740 视口检查；工作区无页面横向溢出，手机讨论/成果切换、导航开关、草稿恢复完成 |
| 表单归属 | 打开表单后历史导航切换探索，再保存会阻止错误归档，保留原输入；已实际操作 |
| Agent 状态 | 新问题只保存讨论，不自动制造等待回答的任务；旧待办继续保留与显示原任务 |
| 语音 | 仅检查入口、转写说明与未开启麦克风状态；未实际录音、识别或朗读 |
| 文件下载 | 内置浏览器点击后未观察到下载事件或落盘文件，尚未验收；复制路径已经用于真实接续 |
| 内容与应用检查 | `npm run check` 通过：23 项通过、1 项 Docker 默认跳过；构建、29 页 646 个本地引用通过 |
| 独立 Docker 专项 | `ATLAS_TEST_DOCKER=1 node --test tests/workspace.test.mjs`：7 项通过，含真实执行、取消与结果保存 |

报告的数值是固定工作量、等速均分、无额外协调开销下的模型求值，不是多人团队性能测量。独立 Agent 资产审阅不等于真人试学；学习方法适用性自查也不证明学习效果。

## 本轮修复

新增「交给自己的 Agent」「带回 Agent 成果」及 `/with-agent/` 使用入口；复用现有权限和成果版本接口，没有引入 Agent 运行时。修复导航选中状态、手机侧栏焦点边界与过期语音回调的目标归属；语音修复尚待真机复核。

导出只包含所选版本自身的运行元数据，不能按成果 ID 附加旧版本的私人任务输入。相应隐私回归测试通过。文件导入先填入表单，保存前绑定原探索；不因切换页面误写入其他探索。Markdown 渲染补齐表格，仍将原始 HTML 和资源链接作为文本处理。

## 证据保存与后续

原始 CLI 日志、提示、最终回复、两个会话产生的完整文件，以及独立浏览器测试数据库，保留在本机 Git 忽略的 `.local/acceptance/2026-09-08/`；`codex-clone/` 是会话后的文件快照，`codex-evidence/` 保存原始日志。没有写入用户正在使用的探索数据库。

原始日志包含本机路径和合成验收探索的来源 ID，不自动公开。历史命令的临时绝对路径保留原样；`verify_assets.py` 是绑定原快照文档与 Git 状态的一次性验收器，不能用于已经演进的主仓库。公开可复用示例应另做有来源的派生版本，不能静默修改冻结输出。运行脚本当前面向 macOS/Linux/WSL；原生 Windows 未测试。

仍待：真机软键盘、语音、弱网和浏览器下载兼容；真人试学；Pi/OpenCode 实际会话；Dev Container 工具启动；有状态数据的备份恢复；跨架构及新版线上部署。本次未提交、推送或部署。

## 网页直接提问与输入模式

2026-09-08 后续修正。通过同一项目的本地 Codex CLI 薄桥接，真实浏览器在独立测试数据库中执行：

- 输入“我想要弄懂第一性原理”，按 Enter 提交；页面先出现真实运行状态，随后讨论中出现 `Codex · AI` 的解释、例子及边界。
- 输入“这和普通的拆解问题有什么区别”，Shift+Enter 确认仅换行、消息数不变，再 Enter 发送；真实回答沿用了前文午餐例子，明确普通拆解与检查前提的关系。
- 在 390×844、320×740 视口切换语音模式，主按钮分别占据输入区整行，390 宽时实际宽 336、高 88；刷新后语音仍选中，麦克风未开启，页面无横向溢出。
- 长回复出现后，桌面输入框仍位于 720 高视口内（底边约 682），手机 740 高视口内底边约 711；正文独立滚动。
- 真实 Codex 任务处于 running 时点击停止；页面和数据库均为 cancelled、租约清空，未产生迟到回复。
- 桌面 Meta+Shift+Space 实际触发语音入口保护（打开成果表单时要求先关闭），没有开启录音。开始/停止、错误、输入法组合、迟到转写隔离由可控语音接口测试覆盖；不等于真机录音验收。

最终 `npm run check`：37 项通过、1 项 Docker 默认跳过，构建及 29 页 646 个本地引用通过。独立 Docker 专项 7 项通过。新增验证覆盖原子消息/任务、并发重试唯一、后续上下文、跨探索隔离、租约续期/过期、取消、停机竞态和 CLI 环境。

本机详细证据在 `.local/acceptance/2026-09-08/direct-chat.sqlite`、`direct-chat-probe.json`、`direct-chat-check.log`、`direct-chat-docker-check.log`。测试问题写入独立数据库；另按用户本次请求，在其原有“第一性原理”探索中使用显式补答入口。

此桥接仅使用已登录的本地 Codex CLI，关闭工具、宿主 Skill 与桌面环境注入；不把本机账号作为公共云模型服务。语音仍依赖浏览器，真机录音、软键盘和新版线上未验收。

## 四个场景与统一工作区

2026-09-08 后续验收，使用独立数据库 `.local/acceptance/2026-09-08/scenes.sqlite`，本轮合成提问关闭 Codex，避免把导航测试变成额外模型调用。测试不写入用户正在使用的 `.data/atlas.sqlite`。

真实浏览器完成：

- 从未发送私人草稿进入“一起探讨”，主区域显示共享列表及空状态，URL/导航选中态改变；发布一段测试共同讨论后列表出现该记录。回到新探索仍保留原私人草稿。
- 搜索“阿姆达尔”，在同一壳内阅读条件、来源和解释，带回原草稿；文字未被覆盖、引用可见。刷新后引用与草稿恢复，没有自动发送。
- 对现有问题选择并行实验、记录预测并运行，得到 2.50×；再运行因果模型，保存独立结果。返回目录再回到探索，未发送草稿和结果保留。
- 保存一份笔记、沉淀为私人知识，在主阅读区打开，修改到 v2 并看到 v1/v2 历史。公开词条旧地址的 `#edit` 仍打开修订表单，测试提案提交保存在独立数据库，不改变种子正文。
- 从知识深链进入，回到提问、去实践再浏览器后退，恢复 conversation 与正确 URL；从根首页开始，在 pushState 后辅助入口仍指向正确路径。
- 首问提交后立即继续输入第二条草稿，再切换知识库并返回，新分配的探索中保留第二条未发送文字，标题仍是用户的问题。
- 390×844、320×740 视口，切换场景关闭导航抽屉并聚焦标题；共享讨论可直接返回列表。页面宽度分别等于 390/320，无横向溢出；手机讨论输入区底边分别为 844/740。知识搜索无结果时可把概念带回当前问题，未自动发送。
- 在共享探索中打开私人知识草稿并点击带回，页面明确阻止，保持私人内容不进入共同讨论。
- 最后浏览器日志未见 error/warn；已重置临时视口。以上是桌面浏览器的响应式验收，不是真机触摸、录音或软键盘验收。

本轮 `npm run check`：49 项通过、1 项 Docker 默认跳过，构建与 29 页 503 个本地引用通过。新测试覆盖共享创建默认范围、非法值、跨用户可见性/加入/幂等/回滚，知识引用问题标题，以及路由/返回来源/旧深链与正文转义。独立 Agent 静态复核发现的场景竞态、表单归属和分步重试问题已修正；未把静态复核写成弱网真实验收。

日志：`.local/acceptance/2026-09-08/scenes-check.log`；合成数据库摘要：`scenes-probe.json`。本轮没有修改 Docker 执行器或 CLI 执行逻辑，因此不重复记载此前真实 Docker/Codex 测试为本次运行。

四个主场景及知识入口共用同一工作区。辅助导学、旧帖子和旧实验观察仍有独立页面，数据及身份未迁移。没有线上发布；真机语音/软键盘、弱网、跨设备与真人试学仍待。


## 我的探索直接提问

2026-09-08 再次按用户实际操作修正：历史搜索不能成为主提问框。复用唯一的文字/语音 composer，在我的探索顶部直接输入，Enter 新建私人探索并启动已有 Codex。历史列表与折叠搜索留在下面。

本轮真实浏览器在独立 `.local/acceptance/2026-09-08/mine-question.sqlite` 中完成：

- 从 `?view=mine` 输入“我想弄懂第一性原理，请用一个日常例子开始。”并按 Enter，直接进入新探索，收到 Codex 用咖啡说明第一性原理的回复，全程未点击新建。
- 将这段测试探索设为共享，留下未发送草稿；进入我的探索后保留旧 `s` 作为返回位置，再用 Enter 提出另一个问题。实际新建私人探索并收到第二条真实回复，原共享探索仍只有原来的一问一答。
- 新问题草稿和原讨论草稿在知识库往返、返回旧探索与刷新后分别恢复。整个页面只有一个 composer DOM，未复制语音控制器。
- 390×844 视口中，首页语音主按钮实际为 322×88；刷新保留语音模式但未开启麦克风。320×740 下无横向溢出，Shift+Enter 仅换行；查找历史只筛选记录，没有发送或覆盖主提问草稿。最后重置视口，浏览器未见 error/warn。
- 数据库共两段探索、两条用户问题、两条 `agent:codex` 回复，两个任务均 succeeded。未发送的测试草稿没有进入消息，证据在 `mine-question-probe.json`。

`npm run check` 49 项通过、1 项 Docker 默认跳过，构建及 29 页 503 个本地引用通过，日志 `mine-question-check.log`。独立静态审阅指出的迟到请求切换旧探索手机视图已修正。本轮没有修改 CLI 或 Docker 执行逻辑；未跑 Docker 专项，没有写入用户数据库或部署线上。真机录音与软键盘仍未验收。


## 2026-09-08 · 环境可以创建与共同维护

本次使用独立 `environments.sqlite`（localhost 与 127.0.0.1 分别作为两位参与者），没有向用户数据库写入测试资料。

- 实际从实践页描述“模拟抛硬币，比较 1000 次与 10000 次试验”，创建私人环境 v1，保存 Dockerfile/run.py/README。未点击运行、没有生成假 Agent 任务。
- 页面复制需求与文件成功；保存固定 v1 快照到新的私人探索。独立维护 Agent 在全新目录检查包、完善代码，使用原生 Docker 实际构建并运行两次：1000 次 480 正面、10000 次 4990 正面；输出一致。linux/arm64、Python 3.13.15、UID/GID 1000，只读写入探测得到 errno 30，全部命令退出 0。未验证其他架构、多种子或真实硬币，也没有把单次结果写成普遍保证。
- 通过环境包粘贴导入并预览，保存为原环境 v2，包含 VERIFICATION.md。数据库逐文件比较证明与 Agent 返回包完全相同，v1 与原探索的 v1 文件快照保持原样。
- 在页面明确分享 v2；另一位参与者仅看到 v2 的历史，未看到私人 v1。原创建者派生出自己的私人分支，保留可见的源环境 v2 引用。
- 实际两人同时编辑 v2：参与者先加入共建，保存 v3；创建者的旧表单提交收到冲突，输入未丢失，比较 v3 并合并后保存 v4。没有覆盖对方的修改。随后从历史页单独公开 v1，页面与服务都确认只有该历史版本得到授权。
- 空白起点创建化学实验需求草稿，表单关闭/刷新后的用途、起点与文件恢复。另在实际 390×844、320×740 视口创建植物生长环境；无横向溢出，320 宽保存按钮为 246×48 px。真机键盘与录音未测试。
- 9 项独立服务测试验证两位参与者、显式加入/撤回、基础版本冲突、幂等、派生与来源权限、文件路径/目录碰撞、私钥/大小边界、提交时权限重查，以及公开指定历史版本只解锁对应快照。文件扩展名不限制实验领域。
- 本轮 `npm run check`：61 通过、1 个已有 Docker 专项默认跳过，构建及 29 页 503 个本地引用通过。实际环境包 Docker 验证单独完成；固定网页 runner 与 CLI 对话桥接没有改动，未重跑其 Docker 专项或真实 Codex 模型调用。Skill 校验与差异检查通过。

归档：`.local/acceptance/2026-09-08/environment-roundtrip/` 中 `from-web-v1.json`、`probe.json` 和 `actual-build/returned.json`、`actual-build/logs/commands.json` 等；检查日志为 `environments-check.log`。旧记录与原始日志不清理。本次往返验收使用复制/粘贴，JSON 文件选择器和下载落盘未列为通过。

边界：网页创建的是可维护的环境定义，不自动构建自定义容器；实际构建仍由已有 Agent 和原生工具完成。没有自研 Agent、扩建云执行器或部署线上。本地 8080 更新前保留数据库备份，通过追加迁移新增表，原探索和记录保留。迁移前后对所有原表逐行摘要比较相同，用户数据库没有验收环境；只在隔离数据库留下合成记录。


## 2026-09-08 · 原生 Codex 与共同工作浏览器

验收使用独立服务 8081、数据库 `.local/acceptance/2026-09-08/stream-browser.sqlite`，容器命名空间 b016701b61bb。主探索 f05bf89e-0db1-41d6-a1d6-f36bca8b94a6；全部问题为合成测试，未向用户探索发问。

| 场景 | 实际结果 |
| --- | --- |
| 原生流式内容 | Codex 0.153.4 app-server 的中文消息片段与 MCP 状态进入页面；独立原生首字验证为约 7.7 秒/8 个 delta，产品任务逐步展示；不是预写进度或整答轮询 |
| 创建网页 | 真实任务保存 coin-experiment/index.html 与 README，启动 Python HTTP 服务，打开 localhost:8000 并点击一次；约 85 秒，14 个公开过程条目、47 次持久快照 |
| 人机共享页面 | 维护者经 CUA 在画布点击第二次；下一原生任务直接读到 2/1/50.0%，没有刷新或点击，约 20 秒 |
| 修改并预览 | 下一原生任务备份 index-v1.html，新增 localStorage 笔记输入并在新标签打开，约 62 秒；面板自动跟随，原页面仍在 |
| 输入与连续性 | 原生文字框成功写入中文；连续安全重连和知识库往返保留计数；区别 UI transport 重连与真正重新导航 |
| 手机布局 | 390×844、320×740 视口；工作面板入口、停止、浏览器、文字输入可见，320 宽没有横向溢出，输入后顶部导航位置仍为 0 |
| 真取消 | 原生执行 90 秒每秒写日志，运行中从手机面板停止；任务 cancelled、租约清空、容器实际退出，0 迟到回复，日志 1155 字节后不再增长 |
| 文件恢复 | 下一原生任务从原 README 启动服务，确认 v1、新版与 README 前后校验相同；浏览器重新打开实验，未重跑已取消脚本，约 52 秒 |
| 权限与实例 | 未授权浏览器/事件接口 404；跨源 WebSocket 403；另一探索实际运行于不同 CDP upstream；测试命名空间与用户数据分离 |

协议先用真实模型调用 synthetic_echo、synthetic_write 验证，再完成上述 AIO 与 CUA 场景。初轮无工具输出不记成功；修复 code_mode_host、approve 策略和本机/远端权限语义后才通过。上游 browser-ui destroy 的远端关闭副作用经源码审阅发现，适配先断 transport 再清理；并记录外部导航后画面重新连接的兼容路径。

自动检查：`native-browser-final-check.log` 为本轮 `npm run check`，97 通过、1 默认 Docker 跳过，构建及 29 页/503 引用通过。`native-browser-docker-regression.log` 为额外真实 Docker 回归，11 通过无跳过。`browser-security.json`、`browser-cancellation.json` 为本轮精简记录；测试数据库保留公开过程、结果与状态，原始账号凭据不进入验收输出。

本地 8080 更新前确认没有排队/运行任务并备份数据库；追加 0005 后原 9 条消息仍在。没有发布线上。视口与鼠标/文本操作不等于手机真机触摸、软键盘或录音；触摸适配代码已有但真机待验收。云端账号、共享控制争用、弱网长会话、生产沙箱与完整备份恢复仍需另验。工作目录不自动导入 SQL 成果历史；依赖审计与镜像边界见 [浏览器接入](BROWSER_WORKSPACE.md)。
