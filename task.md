# 路线与进展

最后更新：2026-09-08。当前定位见 [PRODUCT.md](PRODUCT.md)，组织设计见 [探索与协作设计](docs/EXPLORATION_SERVICE.md)。历史计划不能替代本节当前决策。

## 当前目标与范围

从问题出发，引导、科普、自由探讨和实践，积累可长期维护的知识与探索资产。用户可以 clone，在自己的 Agent 中工作；项目通过 Skill、配置和说明接入已有开源 Agent，不建设全套运行时。

**当前优先项：接入原生 Codex 流式过程与可共同操作的工作浏览器，验证创建、交互、取消和恢复；继续维护知识与环境共建路径。**

- 模型、登录、会话、规划、工具循环与调度由所选 Agent 提供。Codex CLI 已完成实际验收；Pi 与 OpenCode 为参考入口，共用 `.agents/skills/knowledge-atlas/SKILL.md`。
- 本地网页已接通原生 Codex app-server 流式回复、工具过程及 AIO 共同工作浏览器；托管服务尚未接通。`waiting_provider` 是兼容的已保存待办，没有后台推理。
- Git 自有知识/探索/环境与服务 SQL 数据分清归属；没有自动双向同步。新目录暂未自动进入网页索引。
- 手机、语音、远程会话优先复用所选工具；已有网页体验仍需真机验收。
- 项目组织工作不迁移或公开用户研究；研究目录独立维护。

## 已完成的底座与组织

- [x] 产品原则：问题引导、逐步科普、共建、自由讨论与实践；正文格式自由。
- [x] 调研教育/知识站点与学习方法，区分效果证据与设计借鉴，保存为方法审阅依据。
- [x] 已有网页工作区、讨论、分享、显式偏好、成果版本与历史。
- [x] 已有内置模型、本地受限 Docker/Python、幂等、租约、取消与有界恢复。
- [x] 已有知识私稿、修订、来源/范围/关系、按版本分享与目录导出。
- [x] 重写 README/PRODUCT/AGENTS 与设计文档，明确 clone 和外部 Agent 的正式路径。
- [x] 增加 `knowledge/`、`explorations/`、`environments/` 维护入口，以及 `.local/` 的私人工作约定。
- [x] 增加一份标准 Skill 与 Codex/Pi/OpenCode 接入说明；Codex 两次真实会话已验收，其他工具仅核实发现路径。
- [x] 沉淀环境定义、镜像、实例、持久数据、运行证据的维护边界与官方资料。
- [x] 提供 Python 标准库 Dockerfile 与可选 Dev Container；环境实际验证见下方本轮记录。

## 下一步：走通可接续探索

- [x] 在全新本地快照 clone 中用 Codex CLI 0.153.4 完成真实会话，确认 Skill 加载、资料发现、实际运行和成果保存；边界见 [验收记录](docs/ACCEPTANCE.md)。
- [ ] 按使用需求再验收 Pi/OpenCode；不把共享 Skill 的发现路径当成已实测。
- [x] 第二个全新 Codex 会话从文件恢复同一探索，比较 80% 与 50% 条件，实际 Docker 运行并保留旧证据。
- [x] 用并行效率问题形成探索、知识修订、运行证据与方法自查；原始会话和来源信息留在本机验收档案。
- [ ] 从已验收资产整理可共享示例，清理一次性验收器依赖，记录派生关系；继续引入真实使用者的问题。
- [x] 网页材料复制 → 自己的 Codex → Docker → Markdown/HTML 导入 → 成果修订与历史完整验收。
- [ ] 在兼容工具中验收 Dev Container 进入、编辑与退出；按真实需求增加环境，不预设全套镜像矩阵。
- [ ] 首个有状态探索出现时，选择所需卷/目录或外部存储，做一次备份到新实例的恢复验证；之后再判断是否需要 Compose/大文件管理工具。

验收结果应让另一位参与者仅凭 clone 和有权访问的资产继续工作，不依赖原 Agent 的隐藏聊天历史。不把 Docker 构建通过或 Skill 格式合法冒充完整使用链路通过。

## 后续项目维护

- [ ] 根据实际内容逐步统一种子资料和 Git 自有知识的阅读索引，保持旧 ID/链接；服务正文仍经明确修订导入导出，不做双向覆盖。
- [ ] 按内容需要整理反向引用、专题、别名与重复条目；先用可读关系和现有工具，不先建复杂图谱平台。
- [ ] 通过使用者已有 Agent 执行具体来源核验、反例补充与环境复现任务，记录差异和未解决问题，不建设自己的自动巡检调度。
- [ ] 为已有网页做真机语音、软键盘、弱网、草稿恢复和多人协作验收；薄连接是否需要，由实际使用决定。
- [ ] 按服务实际使用完善账号恢复、备份/恢复和部署；若需要云运行，选现有托管/Agent/隔离能力。
- [ ] 开展真实学习者试用，观察独立解释与迁移表现；学习效果、内容正确性与结构检查分别记录。

## 已撤回的旧路线

以下不是“已完成”，也不是当前待办：自建 Agent Provider/模型代理、自己实现工具规划循环与持久会话、云 Agent 队列和调度器、全套实时语音服务、内置自主知识维护服务。此前代码中的兼容状态和短时执行器保留；不因为已有接口就继续扩大这条路线。

## 验证与进展记录


### 2026-09-08 · 探索服务底座

- 完成上述工作区、持久化、成果、执行与知识修订基础；实现基线为 `06cc418`。
- `npm run check` 通过：内容校验、16 项测试通过、1 项 Docker 专项默认跳过、构建及 28 个页面的 598 个本地引用检查。
- 单独运行 `ATLAS_TEST_DOCKER=1 node --test tests/workspace.test.mjs`：7 项通过，包含真实非 root 容器输出、取消后容器退出、结果持久化及知识目录导出。
- 本地工作区和服务接口返回正常。未进行浏览器交互、移动端真机或语音验收；未验证新版线上部署。
- 澄清当前没有 Pi/Codex/在线模型接入，当时提出的真实 Agent 链路计划已由下述范围修正撤回。

### 2026-09-08 · 建立路线记录

- 创建本文件，区分已完成底座、待实现能力和待验收体验。
- 在 `AGENTS.md` 加入更新约定，后续开发随工作同步维护这里。
- 本次只修改文档，不重复运行应用测试；上面的测试结果属于此前实现基线。

### 2026-09-08 · 定位与维护入口整理

- README 补齐引导、科普、共建、自由探索、手机与语音的产品定位；将“尚未接入真实 Agent”前置，并区分目标体验与当前手工操作流程。
- AGENTS.md 补齐接手阅读顺序、模块入口、数据权威、知识审阅、隐私与版本约束、执行验证和交接更新要求。
- README、AGENTS.md 与本路线文件互相链接；本次仅改说明文档，核对链接、维护入口与差异，不重跑应用测试。
- 当时计划选择并接通真实 Agent；该优先项已由下述范围修正撤回。


### 2026-09-08 · 转向项目组织与外部 Agent

- 按用户纠正撤回自研 Agent 路线，把 clone 后直接使用自己的 Agent 设为正式路径。
- 新增共享 Skill、参考接入、知识/探索目录与环境资产规范；避免重复 Agent 配置及 SQL/Git 双重覆盖。
- 新增固定基础镜像的 Python 标准库环境与可选 Dev Container。只使用原生 Docker，没有新增 runner、模型 SDK 或调度器。
- 更新历史 Agent 接入文档、贡献入口与网页提示，避免继续要求用户等待内置助手；服务对象和历史保留。
- 本轮 `npm run check` 通过：内容校验、16 项测试通过/1 项 Docker 默认跳过、构建及 28 页 598 个本地引用检查；单独 Docker 专项 7 项通过。
- 新环境在 linux/arm64 实际构建，Python 3.13.15、默认 UID 1000；独立目录写入、退出后留存、新实例只读挂载读取与实际停止均通过，见 [环境验证记录](environments/python-stdlib/VERIFICATION.md)。没有把数据留存当成数据库备份演练。
- Skill 的 `quick_validate.py` 通过；校验依赖仅装在 Git 忽略的 `.local/skill-validation-venv/`，未改应用依赖。文档本地链接、Dev Container JSON/路径及 `git diff --check` 通过，README 独立研究部分原样保留。
- Pi/OpenCode 真实会话、Dev Container 工具启动、其他架构、移动/语音真机与新版线上未验收。
- 没有安装或启动外部 Agent；没有配置模型账号；没有发布线上版本。

### 2026-09-08 · Codex CLI 与产品联合验收

- 实际使用已登录的 Codex CLI 0.153.4 在隔离的新 clone 运行两个会话；第二会话从网页复制材料和原文件接续，完成两次 Docker 模型求值与报告/交互页输出，13 个旧运行文件摘要未变。
- 补齐网页交给自己的 Agent、选版本材料预览/复制、成果文件导入与使用说明；新问题不再自动生成假 Agent 待办，兼容保留旧任务。
- 实际浏览器完成提问、内置实验、材料复制、文件读入、Markdown 表格、隔离交互面板、成果 v2/v1 历史、刷新恢复及 390/320 宽视口检查。切换探索后的表单保护实际通过；真机和语音未验收。
- 独立审阅发现并修复当前成果导出夹带旧任务输入的问题；增加版本隐私与 Markdown 安全测试。
- 本轮 `npm run check` 通过：23 项通过、1 项 Docker 默认跳过，构建及 29 页 646 个本地引用通过；单独 Docker 专项 7 项通过。
- 下载按钮在内置浏览器未观察到下载事件或落盘，复制流程已用于真实接续；这项兼容性不记为通过。
- 完整经过、原始证据位置与限制见 [CLI 与产品验收](docs/ACCEPTANCE.md)。原始资产归档 `.local/acceptance/2026-09-08/`，未写入用户数据库、未发布。

### 2026-09-08 · 修正直接提问与输入体验

- 用户指出此前只验收文件交接，页面提问没有 AI 输出。此前验收不能代表直接对话通过；本次将这一条作为首要验收。
- 增加现有 Codex CLI 的本地薄桥接：可用时消息与任务原子入库，真实回复进入讨论，旧问题可显式补答。复用任务生命周期与原生 CLI，不自建模型、工具循环或云 Agent。
- 增加 Enter 发送、Shift+Enter 换行和输入法保护；文字/语音为可记忆的模式，手机语音为主按钮，桌面 Cmd/Ctrl+Shift+Space 开始/停止。刷新不自动开启麦克风。
- 真实浏览器用 Enter 发送“我想要弄懂第一性原理”，收到 Codex 回复；追问“这和普通的拆解问题有什么区别”继续得到有前文上下文的回答。手机 390/320 宽模式切换和记忆通过。
- 修复长回答挤出输入框：讨论区域独立滚动，输入区保持在当前视口内；真实 DOM 尺寸检查通过。
- 新测试覆盖幂等回复、上下文归属、并发重试去重、取消/过期、停机、CLI 环境边界，以及输入与语音状态。最终 `npm run check` 37 项通过/1 项 Docker 默认跳过、构建及 29 页 646 个本地引用通过；独立 Docker 专项 7 项通过，Skill 校验与文档链接/差异检查通过。完整记录见 [验收记录](docs/ACCEPTANCE.md)。
- 真实浏览器运行中取消 Codex 后，任务为 cancelled、租约清空且没有迟到回复。本地 8080 服务已更新；原用户探索的显式补答目前已停止，没有自动重启。
- 真机录音、软键盘与托管版本仍未验收；本地 CLI 账号没有开放到公网。

### 2026-09-08 · 按场景统一产品与交互

- 用户指出“一起探讨”点击没有明显变化、知识库跳独立页面突兀。根因是只筛选左栏，以及两套主页面壳；改为我的探索、共同讨论、知识阅读和实践四个真实主视图，方案见 [产品场景](docs/PRODUCT_SCENARIOS.md)。
- 共同讨论支持主列表、加载/空/错误反馈和明确发布；新讨论分享范围与首条消息原子创建，原私人探索不受影响。
- 知识目录合并展示公开种子与有权访问的 SQL 修订；旧 URL 进入同一工作区。正文、来源、条件、旧提案和版本历史保留。版本引用可移除，带回原草稿且不自动发送，私人知识发送前复核范围。
- 实践选择当前或新私人探索，预测与结果回到同一问题；重试沿用已完成的目标和预测。手机切换关闭导航、可返回来源列表，浏览器后退/刷新恢复正确场景。
- 独立审阅及浏览器验收发现并修复旧请求抢导航、跨场景列表缓存、隐藏引用、相对链接基准和首问保存期间继续输入丢草稿的问题。
- 本轮 `npm run check` 49 项通过、1 项 Docker 默认跳过，构建及 29 页 503 个本地引用通过。真实浏览器完成共同讨论发布、知识搜索/引用往返、实验保存、SQL 知识 v1/v2 修订历史、旧公开提案入口及 390/320 宽导航。详细证据与限制见 [验收记录](docs/ACCEPTANCE.md)。
- 全部合成数据留在独立 `.local/acceptance/2026-09-08/scenes.sqlite`。8080 本地服务已更新，保留原数据库；未重跑真实 Codex 调用或 Docker 专项，既有底层执行逻辑未改。没有发布线上。
- 旧导学、旧帖子与旧实验观察仍为辅助页面，未合并身份或搬迁历史。真机录音、软键盘、弱网、跨设备和真人试学仍待验收。

### 2026-09-08 · 我的探索直接承接提问

- 用户指出“我的探索”输入后回车没有启动 Agent。上一轮把主入口做成历史搜索，违反直接表达意图的目标；撤回这个交互。
- 顶部改为真实提问框，复用现有 Enter/输入法保护、文字/语音模式与发送流程；历史列表放在下面，查找历史为次要操作。无需先点击新建。
- 首页草稿与原探索草稿独立保存；即使保留旧共享探索的 `s`，回车也新建私人探索并启动 Agent。切换时移动同一表单，避免重复语音控制器或销毁监听器；迟到响应不能切换其他探索的手机视图。
- 在独立 `mine-question.sqlite` 的真实浏览器里，从我的探索按 Enter 发送“我想弄懂第一性原理”，收到 Codex 的咖啡例子解释，未点击新建。原草稿/新草稿在知识库往返和刷新后保留；从旧共享探索返回我的探索再次 Enter，实际创建新的私人探索并收到第二条回复，旧讨论消息不变。390/320 宽布局、语音模式记忆与历史搜索通过。验收细节见 [验收记录](docs/ACCEPTANCE.md)。
- 本轮 `npm run check` 49 项通过、1 项 Docker 默认跳过，构建和 29 页 503 个本地引用通过。本地 8080 前端已更新，用户数据库不写测试数据；真机语音/软键盘仍未验收，未部署线上。

### 2026-09-08 · 参与者创建与共建环境

- 用户指出实验环境需要按不同实验新建、允许参与者共同构建。实践页已将新建环境、复用和共建目录放在主入口；描述用途即可从 Python 或空白起点建立私人草稿，不要求先写 Dockerfile。
- 环境作为独立 SQL 资产，追加迁移 `0004_grey_wolfpack.sql`；支持文件编辑/包导入、加入共建、基础版本修订、派生、单独分享当前或明确选定历史版本。未公开的私人历史不会随分享开放，旧私有快照不能借探索分享间接公开。
- 环境可冻结到探索，后续修订不改变旧文件；交给自己的 Agent 复制/导出所选包，构建检验后回原环境审阅导入。更新 README/PRODUCT/AGENTS、架构、资产维护和共享 Skill，保持现有 Agent 与原生工具的边界。
- 真实浏览器创建硬币环境 v1 → 独立维护 Agent 实际 Docker 构建/两次运行 → 包导回 v2；逐文件核对与返回包一致，v1 和探索快照不变。两位参与者真实加入/编辑/版本冲突/合并到 v4、独立分享与派生通过。空白环境草稿刷新恢复、390/320 宽新建通过。
- 本轮 `npm run check` 61 通过、1 个已有 Docker 专项默认跳过，29 页/503 引用通过；Skill 校验、文档链接与差异检查通过。实际环境包 Docker 测试已完成，未重跑固定 runner 专项和 Codex 调用。详细依据与日志位置见 [验收记录](docs/ACCEPTANCE.md)。
- 本地 8080 更新并保留原数据；测试仅在独立数据库。未部署线上，未新增通用网页/云环境执行器。真机语音/软键盘、文件下载落盘与文件选择器、其他环境及有状态恢复仍需分别验收；后续由真实实验需求推动新的共建环境。

### 2026-09-08 · 合并重复的提问入口

- 用户指出「我的探索」与「开始新的探索」功能重复。移除侧栏的新建按钮，默认首页与无 s 的 explore 统一为我的探索；已有探索链接继续打开对话，预置问题入口保留。
- 对话内只提供上下文「新问题」，与 Cmd/Ctrl+K、侧栏我的探索共用入口；保存旧对话与首页草稿，不清空、不自动创建数据。首次合并时只在首页草稿不存在的情况下接回旧空白页草稿。
- 独立浏览器实际从根首页 Enter 建立探索，验证新问题、回到旧对话、快捷键往返中两份草稿保留且只有一个 composer。320 宽对话与首页没有横向溢出；已在首页点击手机导航中的我的探索会收起侧栏并聚焦输入。顶部 Agent 交接在手机沿用工具菜单入口，避免与新问题挤在一行。
- 本轮 `npm run check` 62 通过、1 个已有 Docker 专项默认跳过，29 页/503 引用通过；文档与差异检查通过。验收数据仅在 `.local/acceptance/2026-09-08/navigation-home.sqlite`，日志为 `navigation-consolidation-check.log`。本地前端已更新；未改数据库/执行器、未调用真实模型或部署线上，真机键盘与语音未重验。

### 2026-09-08 · 原生流式过程与共同工作浏览器

- [x] 按用户明确的内置浏览器目标，接入 Codex app-server 0.153.4、browser-ui 0.2.2 和 AIO Sandbox 1.11.0 固定摘要。正文、工具过程、同一 Chrome 的人机交互与工作文件接通；没有新增模型 SDK 或工具循环。选型与维护见 [浏览器接入](docs/BROWSER_WORKSPACE.md)。
- [x] 公开事件快照通过追加迁移 0005 持久化，SSE 推送原生片段/工具状态；重连不会重复调用模型，迟到片段受租约保护。完成正文到达前保留部分回答，历史只展示当前尝试；桌面和手机面板均可停止。
- [x] 真实 Codex 在独立环境创建抛硬币网页、保存 README、启动服务并打开/点击。维护者在面板再点一次后，Codex 正确读取 2 次、1 次正面、50%。后续原生任务保留 index-v1.html 并新增笔记框，面板自动显示新页面；维护者通过原生输入框把中文写进网页。
- [x] 连续重连、知识库往返保持页面内存计数。修复上游 destroy 会关闭远端标签的问题，先断开 transport 再清理。390/320 宽布局与原生中文输入通过；320 宽输入时 headerTop=0、scrollWidth=320，输入框在视口内。
- [x] 真实任务运行每秒追加日志脚本，手机点击停止后 job=cancelled、lease=null、容器停止、没有迟到回复，日志保持 1155 字节。之后 Codex 读取原 README 重启网页，旧/新文件校验不变，未重跑计时脚本。不同数据库/探索隔离及匿名读取、跨源 WebSocket 拒绝通过。
- 本轮 `npm run check` 97 项通过、1 个已有 Docker 专项默认跳过，29 页/503 引用通过；另跑真实 Docker 回归 11 项全部通过。协议、SSE、取消、撤权、配置审计、环境归属和服务锁都有独立临时数据测试。
- 首轮曾因关闭 code_mode_host、auto 审批策略和本机/远端权限表述而不能真正调用工具；完成真实 synthetic MCP 及网页任务后才记接通，未把工具清单或口头回答当成执行成功。
- 8080 已更新并应用 0005，迁移前备份为 `.local/acceptance/2026-09-08/user-before-native-browser.sqlite`；原 9 条消息保留，用户数据库没有测试提问。合成数据仅在 `stream-browser.sqlite`；检查日志为 `native-browser-final-check.log`，Docker 日志为 `native-browser-docker-regression.log`。详细证据见 [验收记录](docs/ACCEPTANCE.md)。
- 尚未验收真实手机触摸/软键盘/语音、弱网长期使用和生产云隔离；工作目录尚未自动变成 SQL 成果版本或自动备份。镜像完整重建源码和传递依赖风险须继续维护，未部署线上。

### 2026-09-08 · 产品提交范围与独立校验

- 将产品组织、环境共建、统一工作区及原生 Codex／共同浏览器接入整理为一次完整提交；独立研究正文、形式化修改及研究路线记录留在本地工作树。
- 从实际暂存区导出独立源码副本，复用已安装依赖运行 `npm run check`：97 项通过、1 项 Docker 专项默认跳过，构建及 29 页／503 个本地引用检查通过；日志为 `.local/acceptance/2026-09-08/commit-check/check.log`。本轮未重跑真实 Codex、Docker 或浏览器验收，上节证据属于接入验收。
- 提交前 `git diff --cached --check` 通过；未包含本机数据库、连接私钥或验收原始数据。目标为产品开发分支，不变更 main 或执行部署。

## 更新约定

实质工作前确认当前优先项。完成阶段、改变方案或发现限制后更新状态、真实验证和下一步，详细设计留在对应文档。只勾选实际完成的范围；设计、格式校验、实际执行、体验验收与线上可用分别记录。

项目路线留在本文件，单次探索进度放自身目录；不要在根路线堆聊天日志。本文件不表示已配置后台定时任务。
